# Minimal Dell iDRAC-shaped OpenAPI 3.x fixture for fast unit tests.
# Mirrors the real spec structure without any of the 64,000-line payload.
openapi: "3.0.1"
info:
  title: "Dell iDRAC Test Fixture"
  version: "7.00.00.00-test"
  description: "Minimal fixture for parser unit tests."

components:
  schemas:
    RedfishError:
      description: "Error payload"
      properties:
        error:
          type: object
      type: object

  securitySchemes:
    basicAuth:
      scheme: basic
      type: http
    X-Auth:
      in: header
      name: X-Auth-Token
      type: apiKey

paths:
  /redfish/v1:
    get:
      operationId: "GET_/redfish/v1"
      summary: "ServiceRoot instance"
      description: >-
        Long description that should NOT appear in Contract A.
        This tests that the stripping process works correctly.
      tags:
        - ServiceRoot
      security: []
      responses:
        "200":
          description: "OK"
          content:
            application/json:
              example:
                "@odata.id": "/redfish/v1"
              schema:
                type: object

  /redfish/v1/Systems/{ComputerSystemId}:
    parameters:
      - name: ComputerSystemId
        in: path
        required: true
        schema:
          type: string
        description: "Path-level param — should be captured."
    get:
      operationId: "GET_/redfish/v1/Systems/{ComputerSystemId}"
      summary: "ComputerSystem instance"
      description: "Another long description with x-longDescription noise."
      x-longDescription: "DMTF metadata — must be stripped."
      tags:
        - ComputerSystem
      security:
        - basicAuth: []
        - X-Auth: []
      responses:
        "200":
          description: "OK"
    patch:
      operationId: "PATCH_/redfish/v1/Systems/{ComputerSystemId}"
      summary: "Patch system"
      description: "Patch description — stripped."
      tags:
        - ComputerSystem
      security:
        - basicAuth: []
        - X-Auth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
      responses:
        "200":
          description: "OK"
        "204":
          description: "No content"

  /redfish/v1/Systems/{ComputerSystemId}/Actions/ComputerSystem.Reset:
    post:
      operationId: "POST_/redfish/v1/Systems/{ComputerSystemId}/Actions/ComputerSystem.Reset"
      summary: "Reset computer system"
      description: "Reset action description."
      tags:
        - ComputerSystem
      parameters:
        - name: ComputerSystemId
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
      responses:
        "200":
          description: "OK"

  /redfish/v1/AccountService/Accounts:
    get:
      operationId: "GET_/redfish/v1/AccountService/Accounts"
      summary: "ManagerAccountCollection instance"
      description: "No required params — should produce empty list."
      tags:
        - ManagerAccountCollection
      parameters: []
      security:
        - basicAuth: []
      responses:
        "200":
          description: "OK"
    post:
      operationId: "POST_/redfish/v1/AccountService/Accounts"
      summary: "Create account"
      description: "Create account — body required."
      parameters: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
      responses:
        "201":
          description: "Created"

  /redfish/v1/UpdateService/Actions/UpdateService.SimpleUpdate:
    post:
      operationId: "POST_/redfish/v1/UpdateService/Actions/UpdateService.SimpleUpdate"
      summary: "Firmware update"
      description: "Trigger firmware update."
      parameters:
        - name: "$filter"
          in: query
          required: false
          schema:
            type: string
          description: "Optional filter — should NOT be captured."
        - name: "X-Correlation-ID"
          in: header
          required: true
          schema:
            type: string
          description: "Required header — should be captured."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
      responses:
        "200":
          description: "OK"
